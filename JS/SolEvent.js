class SolEvent {
    constructor(desc, category, badEvent, points) {
        this.desc = desc;
        this.category = category;
        this.badEvent = badEvent;
        this.points = points;
    }
}